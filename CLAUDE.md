# 项目规则

- 根据 Markdown 题目生成代码时，Claude 生成的代码统一放在对应练习目录同级的 `*_claude` 目录中。
- 默认不覆盖用户原目录中的手写代码。
- Claude 生成的代码默认逐行添加中文注释。
- 参考实现优先保持单文件，并尽量贴近题目学习目标。
- 以当前项目为例：用户手写代码保留在 `week/day1/`，Claude 生成代码放在 `week/day1_claude/`。
